import operator
fname = input('enter the file name:')

fhand = open(fname)
counts = dict()
for lines in fhand:
  words = line .split()
  for words in words:
  if word in counts: 
  count[word] = 1
else:
 count[word] = 1
 counts = sorted(counts.item(),key = operator.itemgetter(1),reverse = true]

 for i in range(10):
  print(counts[i])
